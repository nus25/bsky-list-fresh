{
	"name": "bsky-list-fresh",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy:staging": "pnpm build:public && wrangler deploy --env staging",
		"deploy:prod": "pnpm build:public && wrangler deploy --env production",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "pnpm build:public && vitest",
		"cf-typegen": "wrangler types",
		"build:public": "tsx scripts/build-public.ts"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "0.15.2",
		"esbuild": "0.28",
		"tsx": "4.21.0",
		"typescript": "6.0.3",
		"vite": "7.3.2",
		"vitest": "4.1.5",
		"wrangler": "4.87.0"
	},
	"dependencies": {
		"@atproto/api": "0.19.11",
		"@atproto/syntax": "0.5.4"
	},
	"pnpm": {
		"overrides": {
			"picomatch": "4.0.4",
			"postcss": "8.5.10"
		}
	}
}
